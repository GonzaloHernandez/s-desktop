#ifndef TYPES_H
#define TYPES_H

#include <GL/glx.h>
#include <GL/glu.h>

typedef unsigned char uchar;
typedef unsigned int uint;
typedef unsigned long ulong;

enum AlignmentType{
    LEFT, RIGTH, CENTER
};

#endif // TYPES_H
